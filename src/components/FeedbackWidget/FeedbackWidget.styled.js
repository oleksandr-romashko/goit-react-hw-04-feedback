import styled from 'styled-components';

/**
 * Styled container for the feedback widget.
 */
export const WidgetWrapper = styled.div`
  width: fit-content;
  display: flex;
  flex-direction: column;
  margin-right: auto;
`;
